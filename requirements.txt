# Phase 1 — Baseline Engine + APC (Aire HPC)
# Smoke-tested 2026-07-18: Llama-3.1-8B-Instruct + APC, TTFT ~1.9s
# Do not bump mid-experiment without recording a new baseline run.
#
# Fresh venv on Aire (once):
#   module load python/3.13.0
#   python -m venv .venv && source .venv/bin/activate
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
# Pins that work on Aire (driver CUDA 12.6):
#   - vllm==0.11.0          (NOT 0.24.x — needs CUDA 13)
#   - transformers==4.57.6  (NOT 5.x — breaks tokenizer APIs vllm 0.11 needs)
#   - torch: leave whatever vllm installs; do not force-reinstall a different CUDA build
#   - runtime: export VLLM_USE_FLASHINFER_SAMPLER=0  (set automatically in run_vllm.sh)

vllm==0.11.0
transformers==4.57.6
openai>=1.40.0,<2.0.0

# Phase 2 — pass-through proxy
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
httpx>=0.27.0,<1.0.0

# Phase 4 — rewrite unit tests + embedding fallbacks (optional on Aire)
pytest>=8.0.0,<9.0.0
sentence-transformers>=3.0.0,<6.0.0
numpy>=1.26.0,<3.0.0
